import ToastContainer from './toast/ToastContainer.jsx';
import { success, error, promise } from './toast/toaster';

const toast = {
  success,
  error,
  promise,
};

export { ToastContainer, toast };
